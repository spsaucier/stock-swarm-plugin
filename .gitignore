# Secrets and local config (never commit API keys or live MCP config)
.env

# Local-only tooling (X MCP server clone, venv, credentials)
local/

# Local MCP config — copy from plugins/stock-swarm/mcp.example.json; paths vary by agent
.cursor/
.vscode/mcp.json

# User analysis output (consumer projects)
analyses/

# Local docs (maintainer notes, plans — not shipped with the plugin)
docs/

# Python (legacy / local venv if present)
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# OS and editors
.DS_Store
.idea/
.vscode/
*.swp

# Claude local cache
.claude/

# Node (if added later)
node_modules/
