# Secrets and local config (never commit API keys or live MCP config)
.env
.cursor/mcp.json
.cursor/

# User analysis output (consumer projects)
analyses/

# Local docs (maintainer notes, plans — not shipped with the plugin)
docs/

# Python (legacy / local venv if present)
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# OS and editors
.DS_Store
.idea/
.vscode/
*.swp

# Claude local cache
.claude/

# Node (if added later)
node_modules/
